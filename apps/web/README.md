# NovaOps Web

Next.js frontend for NovaOps, served from the VPS production stack at `https://nova-ops.cloud`.

## Commands

```bash
npm run dev
npm run lint
npm run typecheck
npm run build
```

Production deployment is handled from the repository root with `scripts/deploy-vps-live.ps1`.
